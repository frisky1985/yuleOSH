# GSCR 企标规则 — 专家审查邀请

> **审查主题：** 大研发企标规则汇总 V1.1 → yuleOSH 规则集集成审查
> **审查日期：** 2026-06-25
> **规则总量：** 430 条（C: 186 + C++: 244）
> **复合规则集：** `GscrCompositeRuleSet ("gscr")` — 已注册为默认规则集

---

## 1. 审查范围

本次审查覆盖"大研发企标规则汇总 V1.1"全部 430 条规则在 yuleOSH 平台上的导入与集成：

| 维度 | 数据 |
|------|------|
| 规则文件 | `gscr-c-rules.yaml` (186条 C), `gscr-cpp-rules.yaml` (244条 C++) |
| 规则集类 | `GscCRuleSet`, `GscCppRuleSet`, `GscrCompositeRuleSet` |
| 注册器 | `RulesetRegistry` (单例) — 4个规则集已注册 |
| 默认规则集 | `GscrCompositeRuleSet` (名称: `gscr`) |
| 注册规则集列表 | `misra-c2023`, `gscr-c`, `gscr-cpp`, `gscr` |

---

## 2. 审查点详细说明

### 2a. 规则定义准确性 — MISRA 到 GSCR 映射

**C 语言：**
- 179 条规则有 `mapped_misra_ids` 映射（关联到 MISRA C:2012 / C:2023）
- 7 条规则（GSCR-C-27.1 ~ 27.7，CWE 类别）无 MISRA 映射，`mapped_misra_ids: []`
- ⚠️ **待确认：** CWE 7 条规则完全无 MISRA 对应，其合规判定依赖于人工审查。这些规则是否需要增加 CWE 编号引用？

**C++ 语言：**
- 105 条规则有 MISRA 映射
- 139 条规则无 MISRA 映射（占 57%）
  - 这些规则中大部分属于 C++ 标准库安全类别、容器库、异常处理等 C++ 特有领域，MISRA C++ 覆盖不完整
- ⚠️ **待确认：** 高达 57% 的 C++ 规则无 MISRA 映射，映射空缺的原因是什么？这些规则的合规判定基准是什么？

**逆向映射（MISRA → GSCR）：**
- `GscCRuleSet.map_misra_to_gscr()` 和 `GscCppRuleSet.map_misra_to_gscr()` 已实现
- 支持 MISRA 2012 和 2023 格式的自动转换（`misra-c2012-17.7` → `misra-c2023-17.7`）
- ⚠️ **待确认：** cppcheck 输出的是 `misra-c2012` 格式，是否存在格式不匹配导致映射失败的情况？

### 2b. 严重等级 — S0/S1/S2 分级是否合理

| 严重等级 | C 规则数 | C++ 规则数 | 合计 | 说明 |
|---------|---------|-----------|------|------|
| **S0 (Critical)** | 28 | 22 | **50** | 必须修复，安全关键 |
| **S1 (Required)** | 118 | 127 | **245** | 必须修复或正式评审偏离 |
| **S2 (Advisory)** | 40 | 95 | **135** | 建议修复 |
| **合计** | 186 | 244 | **430** | |

**C 语言 S0 规则分布：**
- 初始化: 1 (GSCR-C-13.1 — 未初始化读取)
- 指针类型转换: 1 (GSCR-C-15.1 — const/volatile 丢失)
- 函数副作用: 1 (GSCR-C-17.1 — sizeof 副作用)
- 控制语句表达式: 1 (GSCR-C-18.1 — 不变控制表达式)
- Switch语句: 1 (GSCR-C-20.1 — break 缺失)
- 函数: 3 (GSCR-C-21.1~21.3 — 隐式声明/返回值/数组参数)
- 内存重叠: 1 (GSCR-C-23.1 — 重叠对象赋值)
- 预处理指令: 2 (GSCR-C-24.1~24.2 — include/宏)
- 标准库: 6 (GSCR-C-25.1~25.6, GSCR-25.2 — 保留宏/ctype/string/返回值)
- 资源: 4 (GSCR-C-26.1~26.4 — 释放/文件流)
- CWE: 7 (GSCR-C-27.1~27.7 — 越界/注入/UAF/空指针/整数溢出)

**C++ 语言 S0 规则分布：**
- 基本概念: 3 (main/局部变量返回/地址生命周期)
- 表达式: 6 (指针关系运算/重叠复制/lambda/越界/空指针/除零/sizeof副作用)
- 语句: 2 (noreturn/void返回值)
- 声明: 2 (自动变量返回/地址生命周期)
- 预处理指令: 4 (预处理指令格式/else/endif/include/宏参数)
- 语言支持库: 1 (delete/new 匹配)
- CWE 高频缺陷: 3 (跨界内存写/释放后使用/空指针解引用)

⚠️ **待确认：** 
- C++ 的 S2 比例较高（95/244，39%），C 仅 40/186（22%）。这是合理的差异吗？
- 部分 MISRA "Required" 规则在企标中标记为 S2，是否有风险下放？

### 2c. 分类体系 — 26个C类别 + 28个C++类别

**C 语言类别（26个）：**

| # | 类别 | 规则数 |
|---|------|-------|
| 1 | 编译和构建 | 2 |
| 2 | 需求可追溯性 | 1 |
| 3 | 编码设计 | 5 |
| 4 | 声明和定义 | 10 |
| 5 | 基本类型 | 7 |
| 6 | 表达式 | 14 |
| 7 | 控制语句表达式 | 9 |
| 8 | 控制流 | 10 |
| 9 | Switch语句 | 8 |
| 10 | 函数 | 14 |
| 11 | 函数副作用 | 2 |
| 12 | 指针和数组 | 9 |
| 13 | 指针类型转换 | 4 |
| 14 | 初始化 | 6 |
| 15 | 类型 | 6 |
| 16 | 标识符 | 3 |
| 17 | 未使用的代码 | 7 |
| 18 | 预处理指令 | 9 |
| 19 | 标准C语言环境 | 1 |
| 20 | 标准库 | 18 |
| 21 | 资源 | 4 |
| 22 | 运行时错误 | 13 |
| 23 | CWE | 7 |
| 24 | 字符集和约定词汇 | 1 |
| 25 | 注释 | 1 |
| 26 | 内存重叠 | 1 |

**C++ 语言类别（28个）：**

| # | 类别 | 规则数 |
|---|------|-------|
| 1 | 未分类 | 24 |
| 2 | 一般原则 | 5 |
| 3 | 基本概念 | 7 |
| 4 | 词法约定 | 4 |
| 5 | 标准转换 | 7 |
| 6 | 表达式 | 24 |
| 7 | 语句 | 4 |
| 8 | 声明 | 16 |
| 9 | 声明符 | 3 |
| 10 | 类 | 9 |
| 11 | 派生类 | 4 |
| 12 | 特殊成员函数 | 8 |
| 13 | 重载 | 2 |
| 14 | 成员访问控制 | 1 |
| 15 | 模板 | 2 |
| 16 | 异常处理 | 14 |
| 17 | 预处理指令 | 13 |
| 18 | 库简介 | 1 |
| 19 | 语言支持库 | 5 |
| 20 | 诊断库 | 1 |
| 21 | 常规实用工具库 | 1 |
| 22 | 字符串库 | 3 |
| 23 | 本地化库 | 1 |
| 24 | 容器库 | 1 |
| 25 | 输入/输出库 | 1 |
| 26 | 算法库 | 3 |
| 27 | 随机数生成 | 1 |
| 28 | CWE 高频缺陷 | 3 |

⚠️ **待确认：**
- C++ 中有 24 条规则分类为"未分类"，这些是尚未归类的规则，需要确认归属
- C 的"运行时错误"（13条）和"CWE"（7条）共用编号 GSCR-C-27.x 段，但在分类上分离。这是设计意图吗？
- C 的"标准C语言环境"和"字符集和约定词汇"等类别仅有 1 条规则，是否可以考虑合并？

### 2d. 自动可检性 — 284条自动可检标记

| 检测方式 | C | C++ | 合计 | 占比 |
|---------|---|-----|------|------|
| **自动检测** (auto_checkable=true) | 179 | 105 | **284** | 66.0% |
| **人工审查** (auto_checkable=false) | 7 | 139 | **146** | 34.0% |
| **合计** | 186 | 244 | 430 | 100% |

**工具分布：**
- cppcheck: C 语言 179 条自动规则
- clang-tidy: C++ 语言 105 条自动规则
- manual_review: 146 条（C: 7 + C++: 139）

**人工审查规则的严重等级分布（C++）：**
- manual S0: 8 条（安全关键但无法自动检测）
- manual S1: 73 条（重要但需人工判断）
- manual S2: 58 条（建议但人工判定）

⚠️ **待确认关键问题：**
- **C++ 人工审查比例高达 57%（139/244）**，这是否代表工具链覆盖不足？有哪些规则可通过扩展 clang-tidy check 自动化？
- C 语言 7 条 CWE 规则（S0）全部需人工审查，这是否有工具能辅助检测（如 CodeSonar / Coverity）？
- 284 条自动可检中，是否存在假阳性率高的规则需要调整策略？

### 2e. 与 yuleOSH 规则集接口的集成质量

**已完成：**
- ✅ `GscCRuleSet` / `GscCppRuleSet` 继承自 `BaseRuleSet` 抽象基类，实现所有接口方法
- ✅ MISRA → GSCR 逆向映射完成（`map_misra_to_gscr()`）
- ✅ 违规翻译引擎 `translate_violations()` 支持多命名空间自动识别（C vs C++）
- ✅ `GscrCompositeRuleSet` 自动合并 C + C++ 规则，作为默认规则集
- ✅ 严重等级分类（S0/S1/S2 → critical/required/advisory）标准化映射
- ✅ 4个规则集已在 `RulesetRegistry` 注册，`gscr` 为默认

**需要注意：**
- `GscCppRuleSet` 缺少 `list_rules_by_severity()` 和 `list_rules_by_category()` 方法（`GscCRuleSet` 有但 `GscCppRuleSet` 未继承）
- C 规则集对 `cppcheck` 的配置返回了 `rules_path` 指向 `gscr-c-rules.yaml`，但 cppcheck MISRA addon 可能不识别企标格式的 YAML（需确认是否正常工作）
- `translate_violations()` 中 C vs C++ 的区分逻辑（`re.match(r'misra-c20', ...)`）可能对某些 MISRA C++ 2008 格式不精确

---

## 3. 需要老陈 👨‍🏫 重点关注审查的领域

### 3.1 C 运行时错误规则（GSCR-C-27.8 ~ 27.20）

13 条运行时错误规则，全部 `S1` 级，全部 `auto_checkable=true`，全部映射到 `misra-c2012-Dir-4.1` / `misra-c2023-Dir-4.1`。

**核心问题：** 13 条不同的运行时错误场景全部映射到同一条 MISRA 指令（Dir-4.1 "运行失败应最小化"）。这导致了：
- 无法从 MISRA → GSCR 的逆向映射唯一确定是哪个具体运行时错误
- 报告中的违规计数可能无法精确到具体运行时错误类型
- cppcheck 能输出细分错误类型，但映射关系丢失了细化维度

**需要老陈确认：**
1. 是否需要为每个运行时错误子规则增加更细粒度的 MISRA 映射（如使用 MISRA 2023 的新细分规则编号）？
2. 当前 cppcheck 实际报告是使用 GSCR 规则 ID 还是 MISRA 规则 ID？如果是后者，运行时错误的区分是否依赖于 `gscr_message_cn` 字段？
3. 是否需要为 Dir-4.1 的一对多映射建立优先级规则？

### 3.2 CWE 规则（GSCR-C-27.1 ~ 27.7 / GSCR-CPP-28.1 ~ 28.3）

**C 语言 CWE 规则（7条）：** 全部 `S0`、全部 `manual_review`、全部无 MISRA 映射。
- GSCR-C-27.1 越界读取 → CWE-125
- GSCR-C-27.2 越界写入 → CWE-787
- GSCR-C-27.3 操作系统命令注入 → CWE-78
- GSCR-C-27.4 释放后使用 → CWE-416
- GSCR-C-27.5 缓冲区操作限制不当 → CWE-119
- GSCR-C-27.6 空指针解引用 → CWE-476
- GSCR-C-27.7 整数溢出/循环溢出 → CWE-190

**C++ CWE 规则（3条）：** 全部 `S0`、全部 `auto_checkable=true`（使用 clang-tidy）。
- GSCR-CPP-28.1 跨界内存写
- GSCR-CPP-28.2 释放后使用
- GSCR-CPP-28.3 空指针解引用

**核心问题：** 同样的 CWE 缺陷，C 语言标记为 manual_review，C++ 语言标记为 auto_checkable。这种差异是否有依据？
- clang-tidy 在 C 模式下是否也能覆盖这些 CWE 检测？
- 如果可以，C 的 7 条 CWE 规则是否有计划自动化？

### 3.3 MISRA 映射一致性

**C 语言 MISRA 映射覆盖完全（179/186），但有以下注意点：**
- 运行时错误 13 条全部指向 Dir-4.1 → 一对多映射失焦
- GSCR-C-4.4 和 GSCR-C-4.5 等 S1 规则映射到多个 MISRA 版本 → 需确认映射完整性

**C++ 语言 MISRA 映射覆盖不足（105/244）：**
- 139 条无 MISRA 映射的规则包括大量 C++ 特有安全领域（异常安全、RAII、模板、容器使用等）
- "未分类"的 24 条规则中，有 16 条有 MISRA 映射，8 条无 → 是否可在分类后再确认映射？
- 需要评估这些规则的其他参考标准（如 AUTOSAR C++14 / C++ Core Guidelines）

### 3.4 自动检测可检性判断

**需要老陈评估的准确性：**
| 规则 | 检查方法 | 现有判定 | 建议关注 |
|------|---------|---------|---------|
| GSCR-C-27.1 ~ 27.7 (CWE) | manual_review | 正确 | 是否可借助 Frama-C / ECLAIR 提升自动化 |
| GSCR-CPP-6.19 (lambda 生命周期) | manual_review | 可确认 | clang-tidy 已有 `lifetime` check |
| GSCR-CPP-6.20 (越界访问) | manual_review | 待确认 | clang-tidy `clang-analyzer-alpha.*` 有覆盖 |
| GSCR-CPP-19.10 (delete/new 匹配) | manual_review | 可自动化 | clang-tidy `misc-misplaced-const` 已支持 |

---

## 4. 集成统计数据汇总

| 指标 | C | C++ | 合计 |
|------|---|-----|------|
| S0 | 28 | 22 | 50 |
| S1 | 118 | 127 | 245 |
| S2 | 40 | 95 | 135 |
| auto_checkable | 179 | 105 | 284 |
| manual_review | 7 | 139 | 146 |
| 有 MISRA 映射 | 179 | 105 | 284 |
| 无 MISRA 映射 | 7 | 139 | 146 |
| 类别数 | 26 | 28 | 54 |
| profile=safety | 186 | 244 | 430 |

---

## 5. 审查方式与时间建议

| 活动 | 建议时长 | 建议形式 |
|------|---------|---------|
| 前置阅读本邀请文档 + 规则 YAML 摘要 | 文档自阅 (0.5天) | 异步 |
| MISRA 映射准确性逐条抽查（抽 20 条） | 1 小时 | 会议 |
| C++ 无 MISRA 映射规则的分类讨论 | 0.5 小时 | 会议 |
| CWE 规则自动化可行性评估 | 0.5 小时 | 会议 |
| 运行时错误 Dir-4.1 一对多映射讨论 | 0.5 小时 | 会议 |
| 审查结论与行动项对齐 | 0.5 小时 | 会议 |

---

## 6. 参考文档

- `gscr-c-rules.yaml` — 186条 C 企标规则
- `gscr-cpp-rules.yaml` — 244条 C++ 企标规则
- `src/yuleosh/ci/rulesets.py` — `GscCRuleSet`, `GscCppRuleSet`, `GscrCompositeRuleSet` 实现
- `src/yuleosh/ci/stages.py` — CI 流水线集成入口
- `benchmark/results/misra-benchmark-report.md` — MISRA 基准测试报告

---

*本文档由 yuleOSH 质量架构助手自动生成，数据基于规则文件静态分析和规则集接口验证。*
