# Software Requirements Specification

This document specifies the software requirements for the sample project.

## REQ-001
The system SHALL provide a command-line interface for users.
The system SHALL log all user actions for audit purposes.
Each requirement has a unique identifier (REQ-xxx).

## REQ-002
The system SHALL support configuration via a YAML file.
Configuration changes SHALL be validated before applying.
Requirements are traced to system requirements.
