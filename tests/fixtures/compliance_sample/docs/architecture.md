# Software Architecture

The sample project follows a layered architecture.

## Components
- Interface layer (include/iface.h)
- Core implementation (src/main.c)
- Tests (tests/test_main.c)

Component boundaries and interfaces are defined in the header files.
Architecture review is conducted and documented for each component.
