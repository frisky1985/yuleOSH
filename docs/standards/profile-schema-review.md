# A1-04 Schema 评审材料 — 评审纪要（草案）

> **评审目标（S1 末）**：以「A-M2 ISO 26262 作者视角」走查 `profile-schema.md`，
> 判定**仅凭该文档能否写出一个新的合规标准 profile**（即 schema 是否自洽、完备、可操作）。
> 本文件为评审**材料/纪要草案**，最终结论需**全员评审会确认**（人工闸，不自动通过）。
>
> 关联文档：`docs/standards/profile-schema.md`（A1-09 交付物）

---

## 1. 走查方法

假设角色：**从未读过 yuleOSH Python 代码的 ISO 26262 标准作者**。

手段：只读 `profile-schema.md`，尝试为 ISO 26262（Part 6 / Part 8）写出一份
结构合法的 `iso26262.yaml`，并回答「能否独立验证、能否被引擎消费」。

判定通过的最低门槛：
1. 作者能明确「文件放哪、叫什么、怎么加载」；
2. 作者能表达自有标准结构（非 SWE 过程域也可）；
3. 作者写错时能得到**带路径**的定位错误；
4. 作者无需跑引擎即可本地校验合法性；
5. 作者确信写出的 yaml 能被 checker **无损**消费。

---

## 2. 走查清单（逐条判定）

| # | 评审问题 | 文档依据 | 结论 |
|---|----------|----------|------|
| Q1 | 文件放哪 / 命名 / 加载方式明确吗？ | §2 文件位置与命名表 | ✅ 明确（`profiles/<name>.yaml`，`load_profile(不带后缀)`） |
| Q2 | 能否定义**非 SWE** 的自有过程域？ | §4.2 备注「area 键自由，不强制 swe.N」 | ✅ 可用 `part6`/`part8` 等自有键 |
| Q3 | 能否定义 BP 与产出证据？ | §4.3 / §4.4 字段规范表 | ✅ `base_practices[].output_evidence[].{type,path}` |
| Q4 | 写错时能否定位？ | §5 加载与校验（带字段路径的 `ProfileError`） | ✅ 缺字段/未知字段均带路径与修复提示 |
| Q5 | 能否不跑引擎本地校验？ | §9 第 6 步 `python -c "load_profile(...)"` | ✅ 无引擎依赖即可验证结构合法 |
| Q6 | 能否确信被引擎无损消费？ | §7 反向序列化语义 + §1 核心不变量 | ✅ `to_template_dict` 还原，area.key 即 dict 键 |
| Q7 | 能否附自定义追溯矩阵模板？ | §9 第 8 步 `register_oem_template` | ✅ 注册表接口立起，消费方按 name 检索 |
| Q8 | 字段约束清晰且自洽？ | §4 全量白名单表 + §10 常见错误 | ✅ 必填/类型/约束一目了然 |

---

## 3. 走查实证：ISO 26262 作者据此产出的 yaml

评审过程中，以 §8.2 的 `iso26262.yaml` 为「作者产出样例」进行了结构合法性自检：
`meta.standard=ISO26262`、过程域 `iso26262.part6`/`iso26262.part8` 各含
`id`/`title`/`description`/`order`、`base_practices[].id` 唯一、`output_evidence[].{type,path}` 齐全。

结论：该样例**无需任何代码改动**即可被 `load_profile("iso26262")` 加载，并由
`ComplianceChecker(profile=...)` 消费 —— 印证 schema 对「新标准零代码接入」目标的支撑。

> ⚠️ **走查中发现的阻断性 bug（已修复）**：初版 `ComplianceChecker.run()` 仅识别 `swe.*` 顶层键，
> 非 SWE 标准（如 ISO 26262 的 `iso26262.part6`）会被整体漏掉、生成**空报告**，原 Q6「无损消费 ✅」
> 属**乐观误判**。已在本轮改动中将 `run()` 改为遍历**除 `meta` 外的所有顶层键**，并新增
> `tests/test_compliance_profile_a104.py::test_iso26262_non_swe_keys_consumed` 固化该回归。
> 修复后 Q6 方才成立。

---

## 4. 自评结论

**schema 已具备「仅凭文档写出新 profile」的能力**，证据：
- 字段白名单 + 必填校验 + 带路径报错（§4/§5）保证作者写错能被引导修正；
- 过程域顶层键自由（§4.2）解除「必须 SWE.N」的隐性约束，ISO 26262 等非 SWE 标准可自然表达；
- 反向序列化无损（§7）保证「人写 yml ↔ 引擎消费 dict」语义一致；
- 操作手册（§9）提供从复制基线到端到端验证的完整路径。

---

## 5. 三项设计决策（已按推荐方案拍板，2026-09-13）

经明总批准「按方案推进」，三项原待评审会决策项已落地面向代码，结论如下：

| # | 决策项 | 拍板结论 | 落地位置 |
|---|--------|----------|----------|
| 1 | `evidence.type` 是否强制白名单 | **Option B：强制校验**（白名单 `{document,source,test,ci,evidence,sil}`） | `profile.py` `_EVIDENCE_TYPE_ENUM` + `_validate`；拼写漂移现在加载即报 `ProfileError`（不再静默假阴性） |
| 2 | 是否加 `order` 字段 | **加：可选 `int`**；缺失则保持 yaml 文档序 | `ProcessArea.order` + `_ALLOWED_AREA_KEYS` + `to_template_dict` + `run()` 按 `order` 排序 |
| 3 | 过程域顶层键命名约定 | **采纳 `标准名.过程域` 约定（推荐，非强制）** | `profile-schema.md §4.2` 写明；引擎对键名不做前缀校验 |

> **关键附带修复（阻断性）**：`ComplianceChecker.run()` 原仅识别 `swe.*` 顶层键，非 SWE 标准
> （如 `iso26262.part6`）会被整体漏掉、生成空报告。已改为遍历**除 `meta` 外的所有顶层键**，
> 并随之移除报告生成处的字母序重排（改为按插入/`order` 序）。这是 A-M2 接入 ISO 26262 的硬前置。

**仍建议**在全员评审会上做最终签字确认（人工闸闭环），但技术决策与实现已在本轮完成并固化回归测试。

---

## 6. 评审结论模板（会中填写）

```
评审日期：____-__-__   评审人：______   结论：□ 通过  □ 通过(附条件)  □ 退回
条件/退回说明：________________________________________________
```

---

*本文件为 A1-04 评审材料草案，提交入库以便评审会直接使用；**评审结论需人工填写**，
不在自动化流程内闭环。*
