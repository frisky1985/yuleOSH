/**
 * @file Gateway_SWC.h
 * @brief CAN Gateway SWC Header — {{ project_name }}
 *
 * Generated by {{ generated_by }}
 * MCU: {{ mcu }} | ASIL: {{ asil_label }}
 */

#ifndef GATEWAY_SWC_H
#define GATEWAY_SWC_H

#include "Std_Types.h"
#include "CanIf.h"
#include "Dem_Types.h"

/* ── Route Table Constants ─────────────────────────────────────────── */

#define ROUTE_TABLE_MAX         (256U)
#define DTC_ROUTE_TABLE_CORRUPT (0xC12001UL)
#define DTC_ROUTE_FWD_FAILED    (0xC12002UL)

/* ── Types ──────────────────────────────────────────────────────────── */

typedef struct
{
    uint32_t    canId;
    uint8_t     sourceBus;
    uint8_t     targetBus;
    uint8_t     enabled;
    uint8_t     padding;
    uint16_t    fwdCount;
} RouteEntry;

/* ── Function Prototypes ────────────────────────────────────────────── */

FUNC(void, GW_CODE) Gateway_Init(void);
FUNC(void, GW_CODE) Gateway_MainFunction(void);
FUNC(void, GW_CODE) Gateway_ProcessRoute(RouteEntry *entry);
FUNC(void, GW_CODE) Gateway_EnterSafeState(void);

#endif /* GATEWAY_SWC_H */
