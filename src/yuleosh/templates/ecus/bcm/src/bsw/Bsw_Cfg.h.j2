/**
 * @file Bsw_Cfg.h
 * @brief BSW Configuration Stubs — {{ project_name }}
 *
 * Pre-configured BSW module parameters for {{ mcu }}.
 * Generated by {{ generated_by }}
 */

#ifndef BSW_CFG_H
#define BSW_CFG_H

/* ── MCU Configuration ─────────────────────────────────────────────── */

#define MCU_CORE_CLOCK_HZ       120000000UL   /* 120 MHz */
#define MCU_BUS_CLOCK_HZ        60000000UL    /* 60 MHz  */
#define MCU_RAM_BASE            0x20000000UL
#define MCU_FLASH_BASE          0x00400000UL

/* ── DIO Configuration ─────────────────────────────────────────────── */

#define DIO_NUM_CHANNELS        16U
#define DIO_CONFIG_DOOR_LOCK_A  (0U)
#define DIO_CONFIG_DOOR_LOCK_B  (1U)
#define DIO_CONFIG_TURN_LEFT    (4U)
#define DIO_CONFIG_TURN_RIGHT   (5U)
#define DIO_CONFIG_BRAKE_LIGHT  (6U)
#define DIO_CONFIG_WIPER_SPEED  (10U)
#define DIO_CONFIG_WASHER_PUMP  (11U)

/* ── PWM Configuration ─────────────────────────────────────────────── */

#define PWM_NUM_CHANNELS        4U
#define PWM_LOW_BEAM_CH         (0U)
#define PWM_HIGH_BEAM_CH        (1U)
#define PWM_DRL_CH              (2U)
#define PWM_INTERIOR_CH         (3U)

/* ── CAN Configuration ─────────────────────────────────────────────── */

#define CAN_NUM_CONTROLLERS     1U
#define CAN_0_BAUDRATE          500000UL
#define CAN_BODY_BUS            CAN_CONTROLLER_0

/* ── LIN Configuration ─────────────────────────────────────────────── */

#define LIN_NUM_CHANNELS        2U
#define LIN_CHANNEL_DOOR        (0U)
#define LIN_CHANNEL_MIRROR      (0U)
#define LIN_CHANNEL_RAIN_SENSOR (1U)
#define LIN_BAUDRATE            19200U

/* ── ADC Configuration ─────────────────────────────────────────────── */

#define ADC_NUM_GROUPS          2U
#define ADC_GROUP_KL15          (0U)
#define ADC_GROUP_KL30          (1U)
#define ADC_VREF_MV             3300U

/* ── Watchdog Configuration ─────────────────────────────────────────── */

#define WDG_TIMEOUT_MS          100U
#define WDG_ALIVE_PERIOD_MS     10U

#endif /* BSW_CFG_H */
