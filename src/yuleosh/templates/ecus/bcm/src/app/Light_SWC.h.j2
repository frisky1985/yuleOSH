/**
 * @file Light_SWC.h
 * @brief Light Control SWC Header — {{ project_name }}
 *
 * Generated by {{ generated_by }}
 * MCU: {{ mcu }} | ASIL: {{ asil_label }}
 */

#ifndef LIGHT_SWC_H
#define LIGHT_SWC_H

#include "Std_Types.h"
#include "Pwm.h"
#include "Dio.h"

/* ── Constants ─────────────────────────────────────────────────────── */

#define LIGHT_OFF                   (0U)
#define LIGHT_ON                    (1U)

#define LIGHT_LOW_BEAM              (0x01U)
#define LIGHT_HIGH_BEAM             (0x02U)
#define LIGHT_DRL                   (0x04U)
#define LIGHT_FOG                   (0x08U)

#define LIGHT_TURN_LEFT             (0x01U)
#define LIGHT_TURN_RIGHT            (0x02U)
#define LIGHT_TURN_IDLE             (0x00U)

/* CAN command bits */
#define LIGHT_CMD_LOW_BEAM          (0x01U)
#define LIGHT_CMD_HIGH_BEAM         (0x02U)
#define LIGHT_CMD_TURN_LEFT         (0x04U)
#define LIGHT_CMD_TURN_RIGHT        (0x08U)
#define LIGHT_CMD_HAZARD            (0x10U)
#define LIGHT_CMD_INTERIOR_ON       (0x20U)
#define LIGHT_CMD_DOOR_OPEN         (0x40U)
#define LIGHT_CMD_BRAKE             (0x80U)
#define LIGHT_CMD_EMERGENCY_BRAKE   (0x100U)

/* PWM channels */
#define PWM_CHANNEL_LOW_BEAM        (0U)
#define PWM_CHANNEL_HIGH_BEAM       (1U)
#define PWM_CHANNEL_DRL             (2U)
#define PWM_CHANNEL_INTERIOR        (3U)

/* DIO pins */
#define DIO_PIN_TURN_LEFT           (4U)
#define DIO_PIN_TURN_RIGHT          (5U)
#define DIO_PIN_BRAKE_LIGHT         (6U)

/* DTC */
#define DTC_LIGHT_PWM_FAULT         (0xC11002UL)

/* ── Function Prototypes ────────────────────────────────────────────── */

FUNC(void, LIGHT_CODE) Light_Init(void);
FUNC(void, LIGHT_CODE) Light_MainFunction(void);
FUNC(void, LIGHT_CODE) Light_SetHeadlight(uint8_t beam, uint8_t state);
FUNC(void, LIGHT_CODE) Light_ProcessTurnSignals(uint8_t lightCmd);
FUNC(void, LIGHT_CODE) Light_ProcessInterior(uint8_t lightCmd);
FUNC(void, LIGHT_CODE) Light_ProcessBrakeLight(uint8_t lightCmd);

#endif /* LIGHT_SWC_H */
