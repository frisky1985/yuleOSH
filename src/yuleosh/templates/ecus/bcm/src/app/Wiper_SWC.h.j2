/**
 * @file Wiper_SWC.h
 * @brief Wiper Control SWC Header — {{ project_name }}
 *
 * Generated by {{ generated_by }}
 * MCU: {{ mcu }} | ASIL: {{ asil_label }}
 */

#ifndef WIPER_SWC_H
#define WIPER_SWC_H

#include "Std_Types.h"
#include "Dio.h"

/* ── Wiper Modes ───────────────────────────────────────────────────── */

#define WIPER_MODE_OFF              (0U)
#define WIPER_MODE_MANUAL           (1U)
#define WIPER_MODE_AUTO             (2U)
#define WIPER_MODE_LOW              (3U)
#define WIPER_MODE_HIGH             (4U)
#define WIPER_MODE_INTERMITTENT     (5U)
#define WIPER_MODE_WASH             (6U)

/* ── Wiper Speeds ──────────────────────────────────────────────────── */

#define WIPER_SPEED_PARK            (0U)
#define WIPER_SPEED_INTERMITTENT    (1U)
#define WIPER_SPEED_LOW             (2U)
#define WIPER_SPEED_HIGH            (3U)

/* ── DIO Pins ───────────────────────────────────────────────────────── */

#define DIO_PIN_WIPER_SPEED         (10U)
#define DIO_PIN_WASHER_PUMP         (11U)

/* ── DTC ────────────────────────────────────────────────────────────── */

#define DTC_WIPER_PARK_FAILED        (0xC11003UL)

/* ── Function Prototypes ────────────────────────────────────────────── */

FUNC(void, WIPER_CODE) Wiper_Init(void);
FUNC(void, WIPER_CODE) Wiper_MainFunction(void);
FUNC(void, WIPER_CODE) Wiper_SetSpeed(uint8_t speed);
FUNC(void, WIPER_CODE) Wiper_SetSpeedWithDelay(uint8_t speed, uint8_t delayStage);
FUNC(void, WIPER_CODE) Wiper_Park(void);
FUNC(void, WIPER_CODE) Wiper_ExecuteWashWipe(void);

#endif /* WIPER_SWC_H */
