/**
 * @file Power_SWC.h
 * @brief Power Management SWC Header — {{ project_name }}
 *
 * Generated by {{ generated_by }}
 * MCU: {{ mcu }} | ASIL: {{ asil_label }}
 */

#ifndef POWER_SWC_H
#define POWER_SWC_H

#include "Std_Types.h"
#include "Mcu.h"
#include "Adc.h"
#include "Dio.h"

/* ── Power Mode Definitions ─────────────────────────────────────────── */

typedef enum
{
    POWER_MODE_INIT  = 0,
    POWER_MODE_RUN   = 1,
    POWER_MODE_STOP  = 2,
    POWER_MODE_SLEEP = 3,
    POWER_MODE_SAFE  = 4
} PowerMode;

/* ── ADC Groups ─────────────────────────────────────────────────────── */

#define ADC_GROUP_VOLTAGE           (0U)
#define ADC_GROUP_VOLTAGE_ALT       (1U)   /* Redundant channel for KL15 */

/* ── DTC Codes ──────────────────────────────────────────────────────── */

#define DTC_VOLTAGE_KL30_UNDER      (0xC11004UL)
#define DTC_VOLTAGE_KL30_OVER       (0xC11005UL)
#define DTC_VOLTAGE_KL15_LOW        (0xC11006UL)

/* ── Function Prototypes ────────────────────────────────────────────── */

FUNC(void, POWER_CODE) Power_Init(void);
FUNC(void, POWER_CODE) Power_MainFunction(void);
FUNC(void, POWER_CODE) Power_MonitorVoltage(void);
FUNC(void, POWER_CODE) Power_RequestSleep(void);
FUNC(void, POWER_CODE) Power_TransitionTo(PowerMode newMode);

#endif /* POWER_SWC_H */
