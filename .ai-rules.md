# AI 驱动项目编写规范与通用目录结构指南

## 一、文档概述

本规范适用于所有引入 AI 辅助开发的软件项目，核心目标是解决 AI 生成代码过程中常见的目录混乱、命名随意、逻辑散落等问题，让 AI 生成的代码天然符合团队工程标准，实现"AI 写代码不打乱工程结构"的开发闭环。

本规范同时作为项目级 AI 系统提示词，放置在项目根目录的 `.ai-rules.md` 文件中，所有 AI 辅助开发工具（CodeBuddy、Cursor、Claude Code 等）均可自动读取并注入会话上下文，从源头约束 AI 的代码生成行为。

## 二、顶层工程宪章（Constitution）

本部分为项目最高优先级规则，所有 AI 生成行为必须严格遵守，下层规范不得与本部分内容冲突。

### 2.1 AI 使用原则

**必须项**

1. 所有 AI 生成的代码必须经过人工代码审查，确认符合规范后才能合并入主分支。
2. 安全相关核心逻辑（用户认证、支付流程、数据加密、隐私数据处理）必须由人类工程师实现核心部分，AI 仅可辅助编写工具类、校验类外围代码。
3. 规范文档与代码变更必须同步更新，任何涉及目录结构、命名规则的调整，必须先修改本文档再执行代码变更。
4. 所有 AI 生成的功能代码，必须配套编写对应单元测试，测试覆盖率不得低于项目预设阈值。

**禁止项**

1. 禁止直接让 AI 生成涉及用户隐私数据的处理逻辑，相关代码必须人工编写并经过安全审计。
2. 禁止让 AI 直接修改数据库迁移文件，所有数据库结构变更必须人工确认后执行。
3. 禁止在未明确目录归属、未创建对应规范说明的情况下，直接让 AI 修改核心业务逻辑代码。
4. 禁止 AI 在项目根目录随意创建零散脚本文件，所有临时脚本必须统一放入指定目录。

### 2.2 代码质量最低标准

1. 所有自定义函数、公共方法必须包含完整的中文 docstring，明确说明功能、入参、返回值与异常场景。
2. 所有对外暴露的公共 API 必须有对应的单元测试覆盖，核心业务接口必须补充集成测试用例。
3. 所有代码必须通过团队统一配置的 Lint、格式化工具检查，不得遗留未解决的规范告警。
4. 所有 AI 生成的代码提交时，必须在提交信息中标记 `[AI-GENERATED]` 标识，便于后续追溯与审计。

## 三、通用项目目录结构规范

本结构为跨技术栈通用骨架，可根据前端、后端、AI 算法等不同项目类型做局部适配，核心层级保持统一。

```
# 项目根目录
├── src/                          # 核心业务源码目录
│   ├── data/                     # 数据处理层（数据加载、清洗、转换逻辑）
│   ├── models/                   # 模型/核心业务层
│   │   ├── architectures/        # 网络结构/核心业务实体定义
│   │   ├── trainer.py            # 训练/业务逻辑执行入口
│   │   └── evaluator.py          # 评估/结果校验逻辑
│   ├── utils/                    # 全局通用工具层
│   │   ├── logger.py             # 结构化日志封装
│   │   ├── seed.py               # 随机种子统一控制
│   │   ├── metrics.py            # 通用指标计算函数
│   │   └── path.py               # 路径解析与管理工具
│   ├── pipeline/                 # 端到端流程编排层
│   │   ├── train_pipeline.py     # 训练流程调度
│   │   └── inference_pipeline.py # 推理/业务执行流程调度
│   └── __init__.py               # 对外暴露统一接口，禁止跨层级直接导入内部实现
├── data/                         # 项目数据目录
│   ├── raw/                      # 原始只读数据，任何人不得直接修改
│   ├── processed/                # 中间处理结果数据
│   └── output/                   # 最终输出结果目录
├── models/                       # 训练产出与权重文件目录
│   ├── checkpoints/              # 训练过程断点文件
│   └── weights/                  # 最终发布的模型权重
├── configs/                      # 分级配置目录
│   ├── dev/                      # 开发环境配置
│   ├── staging/                  # 测试环境配置
│   └── prod/                     # 生产环境配置
├── notebooks/                    # 探索性分析 Jupyter Notebook 目录
├── tests/                        # 测试用例目录
│   ├── test_data.py              # 数据处理逻辑测试
│   ├── test_models.py            # 模型/业务逻辑测试
│   └── test_pipeline.py          # 端到端流程集成测试
├── logs/                         # 结构化日志目录，按时间戳生成子目录
├── docs/                         # 项目文档目录
│   ├── specs/                    # 生效的功能需求与技术设计文档
│   └── changes/                  # 需求变更记录归档
├── scripts/                      # 独立工具脚本目录
├── .ai-rules.md                  # 本 AI 辅助开发规范文件
├── requirements.txt              # 项目依赖声明文件
└── README.md                     # 项目总览说明文档
```

## 四、目录使用强制规则

1. 所有业务代码必须收敛在 `src/` 目录下，禁止在根目录直接编写业务脚本，避免出现"脚本堆砌"的混乱局面。
2. `src/` 下的每个子包必须保留 `__init__.py` 文件，对外暴露清晰的统一接口，例如通过 `from src.data import load_dataset` 导入能力，禁止直接从内部实现文件导入。
3. 数据目录严格执行读写分离，`data/raw/` 目录下的文件禁止任何代码写入，所有修改操作只能在 `data/processed/` 中执行。
4. 配置文件按环境分级存放，禁止在业务代码中硬编码配置参数，所有配置通过环境变量 + 配置文件组合注入。
5. 测试目录与 `src/` 目录结构一一对应，新增业务逻辑时必须同步创建对应的测试文件，保证测试覆盖度。
6. 所有日志必须结构化输出，包含时间、日志级别、模块名、核心消息四个要素，同时记录当前项目的 Git Commit ID，便于问题回溯。

## 五、代码编写通用规范

### 5.1 命名规范

1. 所有目录、文件名统一使用小写蛇形命名法，禁止使用中文、特殊字符与大小写混合命名。
2. 类名使用大驼峰命名法，函数、变量名使用小驼峰或蛇形命名法，全量常量统一使用全大写下划线分隔命名。
3. 业务相关方法名必须携带明确语义前缀，例如查询类方法使用 `getXxx`、操作类方法使用 `handleXxx`、重置类方法使用 `resetXxx`，禁止出现语义模糊的 `doSomething` 类命名。
4. 分层对象命名严格统一：请求参数对象统一以 `DTO` 为后缀，响应返回对象统一以 `VO` 为后缀，数据库持久化对象统一以 `PO` 为后缀。
5. **嵌入式开发代码（C/AUTOSAR）命名规则参考 AUTOSAR 标准要求**：
   - **函数命名**：模块前缀 + 动作 + 对象（大驼峰），如 `NvM_WriteBlock`、`CanIf_Transmit`、`Com_ReceiveSignal`；模块缩写须与 AUTOSAR 标准一致（NvM/Com/CanIf/PduR/Dem/Dcm/E2E/Nm 等）。
   - **宏/常量命名**：全大写 + 模块前缀 + 下划线，如 `NVM_E_OK`、`CANIF_E_PARAM_POINTER`、`STD_ON/STD_OFF`、`E_OK/E_NOT_OK`。
   - **类型命名**：模块前缀 + 语义 + `_Type` 后缀（大驼峰），如 `NvM_BlockDescriptorType`、`Std_ReturnType`、`Com_SignalStatusType`；标准基础类型引用 `Std_Types.h` / `Platform_Types.h`（`uint8/uint16/sint16/boolean`），禁止自造基础类型。
   - **枚举命名**：枚举类型以 `_Type` 结尾，枚举值带模块前缀，如 `NvM_BlockStatusType` 的 `NVM_BLOCK_INITIAL`/`NVM_BLOCK_VALID`。
   - **文件命名**：模块名 + `_` + 内容（大驼峰缩写），如 `NvM_Cfg.h`（配置）、`NvM_Types.h`（类型）、`CanIf_Cb.h`（回调）、`MemMap.h`（内存映射）；配置头文件统一 `_Cfg.h` 后缀。
   - **RTE 接口命名**：`Rte_Read_<信号名>` / `Rte_Write_<信号名>` / `Rte_Switch_<模式>` / `Rte_Start_<Runnable>`，与 ARXML 生成的 Rte.h 严格对应。
   - **SWC/组件命名**：`Swc_<功能名>`（如 `Swc_EngineControl`），Runnable 命名 `<组件>_<周期或阶段>`（如 `Swc_EngineControl_10ms`）。
   - **移植/配置层**：平台相关模块加平台后缀（如 `_Mcal`/`_Ecu`），确保 BSW 与 MCAL 解耦，命名不得出现与 AUTOSAR 标准模块同名但语义不同的自定义缩写。

### 5.2 分层职责规范

1. 入口层（Controller/接口层）仅保留参数接收、权限校验、路由转发逻辑，禁止编写复杂业务判断、事务控制逻辑。
2. 业务逻辑层负责业务校验、事务控制、多数据源组合查询，禁止直接操作数据库底层对象。
3. 数据访问层仅负责数据库读写操作，不承载任何业务逻辑。
4. 工具层仅提供通用能力封装，禁止引入任何业务相关依赖，保证工具类可跨项目复用。

### 5.3 AI 生成代码校验规则

1. AI 生成代码后，首先检查文件存放路径是否符合目录规范，确认归属到对应业务模块目录下。
2. 检查所有函数是否补充完整中文注释，公共方法是否标注入参、返回值的类型与含义。
3. 运行项目统一的 Lint 与格式化工具，确认代码无规范告警。
4. 执行对应模块的单元测试，确认新生成代码不破坏原有功能逻辑。
5. 人工确认安全相关逻辑无漏洞后，再执行提交操作。

## 六、变更管理规范

所有需求变更必须遵循标准化流程，避免 AI 随意修改项目结构：

1. 新建 `changes/[变更名称]` 目录，在其中编写 `proposal.md` 说明变更背景与影响范围，编写 `tasks.md` 拆分具体实现任务。
2. 先更新规范文档中对应的规则内容，再让 AI 按照明确的任务清单生成代码。
3. 变更完成后，将变更文档归档到 `docs/changes/` 目录，保证所有项目调整可追溯。
