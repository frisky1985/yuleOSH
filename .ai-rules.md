# AI 驱动项目编写规范与通用目录结构指南

## 一、文档概述

本规范适用于所有引入 AI 辅助开发的软件项目，核心目标是解决 AI 生成代码过程中常见的目录混乱、命名随意、逻辑散落等问题，让 AI 生成的代码天然符合团队工程标准，实现"AI 写代码不打乱工程结构"的开发闭环。

本规范同时作为项目级 AI 系统提示词，放置在项目根目录的 `.ai-rules.md` 文件中，所有 AI 辅助开发工具（CodeBuddy、Cursor、Claude Code 等）均可自动读取并注入会话上下文，从源头约束 AI 的代码生成行为。

> 📌 **阅读前提（必读）**：本文件描述的是**通用参考架构**，不是当前项目的实际目录结构。
> 应用本规范前，AI 必须先完成项目判型（见「2.0 项目适配与防误理解规则」）：
> 基于项目实际文件/技术栈判断，或采用用户提前输入的说明；无法判定时先提问，禁止盲目套模板。

## 二、顶层工程宪章（Constitution）

本部分为项目最高优先级规则，所有 AI 生成行为必须严格遵守，下层规范不得与本部分内容冲突。

### 2.0 项目适配与防误理解规则（使用本文档前的第一原则）

> ⚠️ **本文档描述的是通用参考架构，不是当前项目的实际结构。**
> 通用目录骨架（`src/models/`、`trainer.py`、`notebooks/`、`utils/`、
> `configs/{dev,staging,prod}` 等）是"可适配的理想模板"，**不是每个项目的现状**。
> AI 必须基于项目实际判断如何应用本规范，禁止把模板目录结构盲目套到任何项目上。

**AI 在动手改结构/建目录前，SHALL 先完成项目判型（三选一，按优先级）：**

1. **项目实际结构优先（最高优先）**：先观察仓库真实状态再决定——
   - 构建/依赖文件：`pyproject.toml` / `package.json` / `go.mod` / `CMakeLists.txt` / `Makefile` / `pom.xml` / `Cargo.toml` 等，据此判定技术栈（Python/Node/Go/C/C++/Rust/Java…）；
   - 已有目录：用 `git ls-files` / 列根目录，看项目**实际已有**哪些目录与文件；
   - 判型结论：项目是通用软件（Web/后端/算法）还是嵌入式（AUTOSAR/裸机/RTOS）？已有结构与本规范哪一版骨架匹配？
   - **已有结构与模板冲突时，以项目已有结构为准**——规范是参考，重构需走变更流程（见第六章），不得因"模板里有 `models/`"就在项目里硬建。
2. **用户提前输入优先（次优先）**：若用户已明确说明项目类型、技术栈或指定目录结构，**以用户输入为准**，模板仅作补充参考，不得推翻用户指定。
3. **无法判定时询问（兜底）**：既无法从仓库判定类型、用户也未说明时，AI **必须先提问澄清**（"这是什么类型项目？目标技术栈？"），禁止猜测后直接套模板。

**AI SHALL NOT：**
- 仅因模板骨架里出现 `models/`、`trainer.py`、`notebooks/`、`requirements.txt`、`utils/` 等条目，就在当前项目创建这些目录/文件——模板条目只是"该类型项目可能用到的结构"，不是"每个项目都必须有"。
- 把通用骨架规则（如"每个子包必须保留 `__init__.py`"）强加到 C/C++ 嵌入式工程，或把 AUTOSAR 规则（`config/` 是 ARXML 输入）套到 Python Web 项目。
- 在未判型、未与用户对齐前，直接按模板大规模新建/移动目录。

**AI SHOULD：**
- 判型结论显式写入会话上下文或任务说明（"本项目为 Python 后端，采用通用骨架；不建 `notebooks/`，因为无数据分析需求"），避免后续步骤重复误判。
- 只应用与项目类型匹配的骨架章节：通用项目读「第三章通用骨架」，嵌入式读「3.1 AUTOSAR」，不混用。

### 2.1 AI 使用原则

**必须项**

1. 所有 AI 生成的代码必须经过人工代码审查，确认符合规范后才能合并入主分支。
2. 安全相关核心逻辑（用户认证、支付流程、数据加密、隐私数据处理）必须由人类工程师实现核心部分，AI 仅可辅助编写工具类、校验类外围代码。
3. 规范文档与代码变更必须同步更新，任何涉及目录结构、命名规则的调整，必须先修改本文档再执行代码变更。
4. 所有 AI 生成的功能代码，必须配套编写对应单元测试，测试覆盖率不得低于项目预设阈值。

**禁止项**

1. 禁止直接让 AI 生成涉及用户隐私数据的处理逻辑，相关代码必须人工编写并经过安全审计。
2. 禁止让 AI 直接修改数据库迁移文件，所有数据库结构变更必须人工确认后执行。
3. 禁止在未明确目录归属、未创建对应规范说明的情况下，直接让 AI 修改核心业务逻辑代码。
4. 禁止 AI 在项目根目录随意创建零散脚本文件，所有临时脚本必须统一放入指定目录。

### 2.2 代码质量最低标准

1. 所有自定义函数、公共方法必须包含完整的中文 docstring，明确说明功能、入参、返回值与异常场景。
2. 所有对外暴露的公共 API 必须有对应的单元测试覆盖，核心业务接口必须补充集成测试用例。
3. 所有代码必须通过团队统一配置的 Lint、格式化工具检查，不得遗留未解决的规范告警。
4. 所有 AI 生成的代码提交时，必须在提交信息中标记 `[AI-GENERATED]` 标识，便于后续追溯与审计。

## 三、通用项目目录结构规范

> ⚠️ **先判定项目类型，再选目录骨架**：本规范同时适用于通用软件与嵌入式（AUTOSAR）工程。
> - **通用项目**（Web/后端/算法/Python/TypeScript 等）→ 用下方通用骨架。
> - **嵌入式 AUTOSAR 项目**（C/C++、MCU、ECU、BSW/SWC、芯片驱动）→ **跳过本节，直接用「3.1 AUTOSAR 工程目录结构」**。
> 禁止把通用骨架的 `models/`、`notebooks/`、`requirements.txt`、`trainer.py` 等概念套到嵌入式工程上。

本结构为跨技术栈通用骨架，可根据前端、后端、AI 算法等不同项目类型做局部适配，核心层级保持统一。

```
# 项目根目录
├── src/                          # 核心业务源码目录
│   ├── data/                     # 数据处理层（数据加载、清洗、转换逻辑）
│   ├── models/                   # 模型/核心业务层
│   │   ├── architectures/        # 网络结构/核心业务实体定义
│   │   ├── trainer.py            # 训练/业务逻辑执行入口
│   │   └── evaluator.py          # 评估/结果校验逻辑
│   ├── utils/                    # 全局通用工具层
│   │   ├── logger.py             # 结构化日志封装
│   │   ├── seed.py               # 随机种子统一控制
│   │   ├── metrics.py            # 通用指标计算函数
│   │   └── path.py               # 路径解析与管理工具
│   ├── pipeline/                 # 端到端流程编排层
│   │   ├── train_pipeline.py     # 训练流程调度
│   │   └── inference_pipeline.py # 推理/业务执行流程调度
│   └── __init__.py               # 对外暴露统一接口，禁止跨层级直接导入内部实现
├── data/                         # 项目数据目录
│   ├── raw/                      # 原始只读数据，任何人不得直接修改
│   ├── processed/                # 中间处理结果数据
│   └── output/                   # 最终输出结果目录
├── models/                       # 训练产出与权重文件目录
│   ├── checkpoints/              # 训练过程断点文件
│   └── weights/                  # 最终发布的模型权重
├── configs/                      # 分级配置目录
│   ├── dev/                      # 开发环境配置
│   ├── staging/                  # 测试环境配置
│   └── prod/                     # 生产环境配置
├── notebooks/                    # 探索性分析 Jupyter Notebook 目录
├── tests/                        # 测试用例目录
│   ├── test_data.py              # 数据处理逻辑测试
│   ├── test_models.py            # 模型/业务逻辑测试
│   └── test_pipeline.py          # 端到端流程集成测试
├── logs/                         # 结构化日志目录，按时间戳生成子目录
├── docs/                         # 项目文档目录
│   ├── specs/                    # 生效的功能需求与技术设计文档
│   └── changes/                  # 需求变更记录归档
├── scripts/                      # 独立工具脚本目录
├── .ai-rules.md                  # 本 AI 辅助开发规范文件
├── requirements.txt              # 项目依赖声明文件
└── README.md                     # 项目总览说明文档
```

### 3.1 AUTOSAR 工程目录结构（嵌入式专属）

> 适用于 AUTOSAR Classic/Adaptive BSW、MCAL、SWC 驱动开发。**本结构优先于通用骨架**，两者冲突时以本节为准。

```
# 嵌入式 AUTOSAR 项目根目录
├── src/                          # 核心源码目录（业务代码一律收敛于此）
│   ├── bsw/                      # 基础软件层（Basic Software）
│   │   ├── services/             # 服务层：通信/诊断/存储/网络管理
│   │   │   ├── comm/             #   通信服务（Com/CanTp/PduR 等）
│   │   │   ├── diag/             #   诊断服务（Dcm/Dem）
│   │   │   ├── storage/          #   存储服务（NvM/Fee/Ea）
│   │   │   └── nm/               #   网络管理（Nm/CanNm）
│   │   ├── ecual/                # ECU 抽象层（CanIf/LinIf/Spi 等）
│   │   ├── mcal/                 # 微控制器抽象层（驱动寄存器，平台相关）
│   │   └── os/                   # 操作系统（FreeRTOS/AUTOSAR OS 适配）
│   ├── middleware/               # 中间件层
│   │   ├── rte/                  # 运行时环境（RTE，ARXML 生成）
│   │   └── dds/                  # 通信中间件（可选）
│   ├── application/              # 应用层（Application SWC）
│   │   ├── <swc_name>/           #   每个 SWC 一个目录（如 engine_control/）
│   │   │   ├── include/          #     头文件（Swc_Xxx.h）
│   │   │   └── src/              #     实现（Swc_Xxx.c + Runnable）
│   │   └── ...                   #
│   └── common/                   # 跨层公共代码（Std_Types/Platform_Types 等）
├── config/                       # AUTOSAR 配置输入（ARXML + 生成配置）
│   └── input/                    # 原始配置：arxml/mcal/ecual/services
├── include/                      # 对外公共头文件（autosar 标准接口）
├── tests/                        # 测试目录（与 src/ 结构一一对应）
│   ├── unit/                     # 单元测试（每模块一个测试文件）
│   ├── integration/              # 集成测试
│   ├── sil/                      # SIL 测试
│   └── hil/                      # HIL 测试（硬件在环）
├── docs/
│   ├── specs/                    # 生效的功能需求与技术设计文档
│   ├── requirements/             # 需求规范（SRS，用 autosar_req_template）
│   └── changes/                  # 需求变更记录归档
├── scripts/                      # 构建/分析/部署工具脚本
├── .ai-rules.md                  # 本 AI 辅助开发规范文件
└── README.md                     # 项目总览说明文档
```

**AUTOSAR 工程专属规则**：

1. **禁止把通用骨架概念套到嵌入式工程**：嵌入式工程**不建** `models/`、`notebooks/`、`requirements.txt`、`__init__.py`（C 工程无 Python 包概念）；`config/` 是 ARXML 配置输入，不是通用骨架的环境分级 `configs/{dev,staging,prod}`。
2. **SWC 目录规范**：每个 SWC 一个目录，命名 `src/application/<swc_name>/`，内部 `include/` + `src/`；SWC 头文件命名 `Swc_<功能>.h`，Runnable 命名 `<组件>_<周期或阶段>`（如 `Swc_EngineControl_10ms`）。
3. **BSW 分层**：BSW 严格分层 `services → ecual → mcal`，上层只调下层公开接口；MCAL 是唯一允许直接操作寄存器的层，命名加 `_Mcal`/`_Ecu` 后缀与标准模块区分。
4. **配置与代码分离**：寄存器定义、模块参数由 ARXML/`_Cfg.h` 生成，手写代码不得硬编码配置值；修改配置必须走 configurator 工具链。
5. **测试对应**：`tests/unit/` 与 `src/` 模块一一对应，新增 BSW/SWC 模块必须同步建测试；HIL 测试标记硬件依赖。
6. **构建系统**：嵌入式工程用 CMake/Makefile 管理，编译目标含 `-DBUILD_TESTING` 开关；禁止在根目录散落编译产物（见第四章第 1 条）。
7. **安全与合规**：涉及 ISO 26262（ASIL）、MISRA C 的模块，AI 生成代码必须标注安全等级与合规检查结果；功能安全相关逻辑须人工审查。

## 四、目录使用强制规则

1. 所有业务代码必须收敛在 `src/` 目录下，禁止在根目录直接编写业务脚本，避免出现"脚本堆砌"的混乱局面。
2. `src/` 下的每个子包必须保留 `__init__.py` 文件，对外暴露清晰的统一接口，例如通过 `from src.data import load_dataset` 导入能力，禁止直接从内部实现文件导入。
3. 数据目录严格执行读写分离，`data/raw/` 目录下的文件禁止任何代码写入，所有修改操作只能在 `data/processed/` 中执行。
4. 配置文件按环境分级存放，禁止在业务代码中硬编码配置参数，所有配置通过环境变量 + 配置文件组合注入。
5. 测试目录与 `src/` 目录结构一一对应，新增业务逻辑时必须同步创建对应的测试文件，保证测试覆盖度。
6. 所有日志必须结构化输出，包含时间、日志级别、模块名、核心消息四个要素，同时记录当前项目的 Git Commit ID，便于问题回溯。

## 五、代码编写通用规范

### 5.1 命名规范

1. 所有目录、文件名统一使用小写蛇形命名法，禁止使用中文、特殊字符与大小写混合命名。
2. 类名使用大驼峰命名法，函数、变量名使用小驼峰或蛇形命名法，全量常量统一使用全大写下划线分隔命名。
3. 业务相关方法名必须携带明确语义前缀，例如查询类方法使用 `getXxx`、操作类方法使用 `handleXxx`、重置类方法使用 `resetXxx`，禁止出现语义模糊的 `doSomething` 类命名。
4. 分层对象命名严格统一：请求参数对象统一以 `DTO` 为后缀，响应返回对象统一以 `VO` 为后缀，数据库持久化对象统一以 `PO` 为后缀。
5. **嵌入式开发代码（C/AUTOSAR）命名规则参考 AUTOSAR 标准要求**：
   - **函数命名**：模块前缀 + 动作 + 对象（大驼峰），如 `NvM_WriteBlock`、`CanIf_Transmit`、`Com_ReceiveSignal`；模块缩写须与 AUTOSAR 标准一致（NvM/Com/CanIf/PduR/Dem/Dcm/E2E/Nm 等）。
   - **宏/常量命名**：全大写 + 模块前缀 + 下划线，如 `NVM_E_OK`、`CANIF_E_PARAM_POINTER`、`STD_ON/STD_OFF`、`E_OK/E_NOT_OK`。
   - **类型命名**：模块前缀 + 语义 + `_Type` 后缀（大驼峰），如 `NvM_BlockDescriptorType`、`Std_ReturnType`、`Com_SignalStatusType`；标准基础类型引用 `Std_Types.h` / `Platform_Types.h`（`uint8/uint16/sint16/boolean`），禁止自造基础类型。
   - **枚举命名**：枚举类型以 `_Type` 结尾，枚举值带模块前缀，如 `NvM_BlockStatusType` 的 `NVM_BLOCK_INITIAL`/`NVM_BLOCK_VALID`。
   - **文件命名**：模块名 + `_` + 内容（大驼峰缩写），如 `NvM_Cfg.h`（配置）、`NvM_Types.h`（类型）、`CanIf_Cb.h`（回调）、`MemMap.h`（内存映射）；配置头文件统一 `_Cfg.h` 后缀。
   - **RTE 接口命名**：`Rte_Read_<信号名>` / `Rte_Write_<信号名>` / `Rte_Switch_<模式>` / `Rte_Start_<Runnable>`，与 ARXML 生成的 Rte.h 严格对应。
   - **SWC/组件命名**：`Swc_<功能名>`（如 `Swc_EngineControl`），Runnable 命名 `<组件>_<周期或阶段>`（如 `Swc_EngineControl_10ms`）。
   - **移植/配置层**：平台相关模块加平台后缀（如 `_Mcal`/`_Ecu`），确保 BSW 与 MCAL 解耦，命名不得出现与 AUTOSAR 标准模块同名但语义不同的自定义缩写。

### 5.2 分层职责规范

1. 入口层（Controller/接口层）仅保留参数接收、权限校验、路由转发逻辑，禁止编写复杂业务判断、事务控制逻辑。
2. 业务逻辑层负责业务校验、事务控制、多数据源组合查询，禁止直接操作数据库底层对象。
3. 数据访问层仅负责数据库读写操作，不承载任何业务逻辑。
4. 工具层仅提供通用能力封装，禁止引入任何业务相关依赖，保证工具类可跨项目复用。

### 5.3 AI 生成代码校验规则

1. AI 生成代码后，首先检查文件存放路径是否符合目录规范，确认归属到对应业务模块目录下。
2. 检查所有函数是否补充完整中文注释，公共方法是否标注入参、返回值的类型与含义。
3. 运行项目统一的 Lint 与格式化工具，确认代码无规范告警。
4. 执行对应模块的单元测试，确认新生成代码不破坏原有功能逻辑。
5. 人工确认安全相关逻辑无漏洞后，再执行提交操作。

## 六、变更管理规范

所有需求变更必须遵循标准化流程，避免 AI 随意修改项目结构：

1. 新建 `changes/[变更名称]` 目录，在其中编写 `proposal.md` 说明变更背景与影响范围，编写 `tasks.md` 拆分具体实现任务。
2. 先更新规范文档中对应的规则内容，再让 AI 按照明确的任务清单生成代码。
3. 变更完成后，将变更文档归档到 `docs/changes/` 目录，保证所有项目调整可追溯。

## 七、yuleOSH 交付红线（本项目专属，优先级等同第二章）

> 本章是前述通用规范在 yuleOSH 项目上的**落地约束**，由实际交付事故沉淀而来。
> 与通用章节冲突时，以本章为准。执行细节见 `.workbuddy/skills/yuleosh-fullstack-verify/SKILL.md`。

### 7.1 零回归校验三连（提交前 SHALL 全绿）

后端 `pytest`、前端 `tsc --noEmit` + `jest` + `next build` **必须全部通过**才允许提交。
任一环节失败，先定位根因，**禁止以"环境问题"为由跳过**——但须先按 7.4 判别是否确为环境所致。

### 7.2 后端 / 前端分笔提交，禁止混合

后端（`src/`、`tests/`）与前端（`frontend/`）**分开 commit**：
- 后端一笔：`fix(api):` / `feat(api):` / `refactor(core):`
- 前端一笔：`fix(dashboard):` / `feat(dashboard):` / `refactor(ui):`

commit message 结构：**根因 → 影响面 → 修法 → 实测结论**（实测结论要写具体数字，如"pytest 99 passed"）。

### 7.3 对外暴露文件的端点 SHALL 做路径安全校验

任何"按参数返回服务器文件"的端点（下载 / 导出 / 读取）必须同时满足：

1. **bare name 校验**：拒绝路径分隔符（`/`、`\`）、dot-segments（`.`、`..`）、隐藏文件，
   以及 `"`,`\n`,`\r`,`\x00`（防 `Content-Disposition` 头注入）。
2. **resolve 后仍在目标目录内**：`(base / name).resolve().relative_to(base)`，
   失败即 400。此步同时拦截 symlink 越界。
3. **错误语义分开**：非法名 → 400；不存在 / 是目录 → 404。不得一律 400。
4. **不得回显内部细节**：异常信息经 `internal_error` 脱敏，禁止把 OS 错误、路径、凭据回显给客户端。

### 7.4 结论前 SHALL 排除环境干扰（防误判）

本项目运行在沙箱内，以下现象**与代码无关**，须先排除再下结论：

| 现象 | 性质 | 处置 |
|---|---|---|
| `next build` 报 `[safe-delete] 操作失败` | 沙箱拦截 `.next` 清理 | 加 `CODEBUDDY_SAFE_DELETE_ENABLED=0` |
| `pytest` 大量 `PermissionError: mkdir pytest-of-*` | 沙箱限制 basetemp | 加 `--basetemp=/tmp/xxx --no-cov -p no:cacheprovider` |
| shell `grep` 对中文/特殊字符空输出 | 编码问题 | 用 Grep 工具或落盘后 Read |
| AI 启动的服务浏览器连不上 | 命名空间隔离 | 由用户在本机终端启动并复核 |

**判别方法**：把**未改动**的既有测试/构建单独跑一遍，若同样失败即为环境问题。
**禁止仅凭失败日志断言"代码有回归"**。

### 7.5 新增依赖 GET query 的端点 SHALL 实测参数可达

路由层若用 `parse_qs(urlparse(path).query)` 取参，必须确认上游把 **完整 path（含 query）**
透传下来。历史上 `handler_helpers` 只传 `parsed.path`，导致全站 GET 参数静默丢失
（轮询 `?task_id` 失效、分页/过滤被忽略），且表现为"走默认分支"而非报错，极难发现。

### 7.6 长列表 SHALL 默认折叠

超过 10 条的列表（证据文件、日志 run、历史版本等）默认只渲染前 10 条，
提供展开 / 收起；**总数仍须可见**（标题或按钮文案标出总量），不得让用户误判数据缺失。

### 7.7 版本号单一事实来源，发版 SHALL 四处同步

发版时以下 4 处版本号必须一致，缺一即视为未完成：

- `pyproject.toml`（权威来源）
- `frontend/package.json`
- `src/yuleosh/__init__.py` 的 `__version__`
- `setup.py`（legacy-only，但不得漂移）

同时更新 `CHANGELOG.md`（按 新增功能 / 修复 / 优化 / 基础设施 归类），
打 **annotated tag** 并推送，必要时创建 GitHub Release。

### 7.8 完成后 SHALL 追加项目 memory

追加 `.workbuddy/memory/YYYY-MM-DD.md`（append-only）：记根因、影响面、修法、可复用经验。
**"验证通过无需修复"的结论也要记**，避免后续重复排查同一问题。

### 7.9 内容条款展示 SHALL 限量 + 折叠

UI / 文档需展示**内容条款类**逐项罗列（条款、要点、特性列表、提示项、说明条目等，
区别于 7.6 的"记录型长列表"如证据/日志/版本）时：

1. **通用条款**：默认最多展示 **10 条**，超出部分折叠并提供展开 / 收起；
   折叠态须标注被折叠数量（如"展开其余 N 条"）。
2. **用户相关内容**：默认最多展示 **3 条**（如"我的项目 / 我的待办 / 与我相关"），
   超出部分折叠；优先折叠次要项，避免首屏被"与我相关"的内容淹没。
3. **总数可见**（与 7.6 一致）：无论折叠与否，列表总量须在标题或按钮文案中可见，
   不得让用户误判信息缺失。

> 与 7.6 的边界：7.6 管"数据记录长列表"（按条数客观增长）；
> 本规则管"人为撰写的条款 / 要点罗列"（内容展示密度）。两者均适用"默认折叠 + 总数可见"。
